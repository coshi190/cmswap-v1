# RPC URLs
RPC_URL_KUB=https://rpc.bitkubchain.io
RPC_URL_JBC=https://rpc-l1.jibchain.net
RPC_URL_BSC=https://56.rpc.thirdweb.com

# Contract addresses (proxies)
BRIDGE_ADDRESS_KUB=0x...
BRIDGE_ADDRESS_JBC=0x...
BRIDGE_ADDRESS_BSC=0x...

# Relayer wallet (must be set as relayer in bridge contracts)
RELAYER_PRIVATE_KEY=0x...

# Database
DATABASE_PATH=./data/relayer.db

# Optional: Dry run mode (no transactions, just logging)
DRY_RUN=false

# Optional: Max gas price caps (in gwei)
MAX_GAS_PRICE_KUB=100
MAX_GAS_PRICE_JBC=100
MAX_GAS_PRICE_BSC=10

# Optional: Starting blocks (used on first run if no checkpoint exists)
# START_BLOCK_96=
# START_BLOCK_8899=
# START_BLOCK_56=
